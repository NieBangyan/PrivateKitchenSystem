<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kitchen.backend_springbook_kichen.mapper.RecipeMapper">

    <resultMap id="BaseResultMap" type="com.kitchen.backend_springbook_kichen.entity.Recipe">
        <id column="id" property="id" />
        <result column="title" property="title" />
        <result column="category_id" property="categoryId" />
        <result column="ingredients" property="ingredients" />
        <result column="steps" property="steps" />
        <result column="cooking_time" property="cookingTime" />
        <result column="difficulty" property="difficulty" />
        <result column="image_url" property="imageUrl" />
        <result column="author_id" property="authorId" />
        <result column="view_count" property="viewCount" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <resultMap id="DetailResultMap" type="com.kitchen.backend_springbook_kichen.entity.Recipe" extends="BaseResultMap">
        <result column="category_name" property="categoryName" />
        <result column="author_name" property="authorName" />
    </resultMap>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM recipe WHERE id = #{id}
    </select>

    <select id="findDetailById" resultMap="DetailResultMap">
        SELECT r.*, c.name as category_name, u.real_name as author_name
        FROM recipe r
                 LEFT JOIN category c ON r.category_id = c.id
                 LEFT JOIN user u ON r.author_id = u.id
        WHERE r.id = #{id}
    </select>

    <select id="search" resultMap="DetailResultMap">
        SELECT r.*, c.name as category_name, u.real_name as author_name
        FROM recipe r
        LEFT JOIN category c ON r.category_id = c.id
        LEFT JOIN user u ON r.author_id = u.id
        WHERE 1=1
        <if test="title != null and title != ''">
            AND r.title LIKE CONCAT('%', #{title}, '%')
        </if>
        <if test="categoryId != null">
            AND r.category_id = #{categoryId}
        </if>
        <if test="difficulty != null and difficulty != ''">
            AND r.difficulty = #{difficulty}
        </if>
        ORDER BY r.create_time DESC
        <if test="offset != null and pageSize != null">
            LIMIT #{offset}, #{pageSize}
        </if>
    </select>

    <select id="countSearch" resultType="long">
        SELECT COUNT(*) FROM recipe r
        WHERE 1=1
        <if test="title != null and title != ''">
            AND r.title LIKE CONCAT('%', #{title}, '%')
        </if>
        <if test="categoryId != null">
            AND r.category_id = #{categoryId}
        </if>
        <if test="difficulty != null and difficulty != ''">
            AND r.difficulty = #{difficulty}
        </if>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO recipe(title, category_id, ingredients, steps, cooking_time, difficulty, image_url, author_id)
        VALUES(#{title}, #{categoryId}, #{ingredients}, #{steps}, #{cookingTime}, #{difficulty}, #{imageUrl}, #{authorId})
    </insert>

    <update id="update">
        UPDATE recipe
        SET title = #{title},
            category_id = #{categoryId},
            ingredients = #{ingredients},
            steps = #{steps},
            cooking_time = #{cookingTime},
            difficulty = #{difficulty},
            image_url = #{imageUrl}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM recipe WHERE id = #{id}
    </delete>

    <update id="incrementViewCount">
        UPDATE recipe SET view_count = view_count + 1 WHERE id = #{id}
    </update>

    <select id="findFirst" resultMap="DetailResultMap">
        SELECT r.*, c.name as category_name, u.real_name as author_name
        FROM recipe r
                 LEFT JOIN category c ON r.category_id = c.id
                 LEFT JOIN user u ON r.author_id = u.id
        ORDER BY r.id ASC LIMIT 1
    </select>
    <select id="findLast" resultMap="DetailResultMap">
        SELECT r.*, c.name as category_name, u.real_name as author_name
        FROM recipe r
                 LEFT JOIN category c ON r.category_id = c.id
                 LEFT JOIN user u ON r.author_id = u.id
        ORDER BY r.id DESC LIMIT 1
    </select>

    <select id="findPrev" resultMap="DetailResultMap">
        SELECT r.*, c.name as category_name, u.real_name as author_name
        FROM recipe r
                 LEFT JOIN category c ON r.category_id = c.id
                 LEFT JOIN user u ON r.author_id = u.id
        WHERE r.id &lt; #{currentId}
        ORDER BY r.id DESC LIMIT 1
    </select>

    <select id="findNext" resultMap="DetailResultMap">
        SELECT r.*, c.name as category_name, u.real_name as author_name
        FROM recipe r
                 LEFT JOIN category c ON r.category_id = c.id
                 LEFT JOIN user u ON r.author_id = u.id
        WHERE r.id &gt; #{currentId}
        ORDER BY r.id ASC LIMIT 1
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(*) FROM recipe
    </select>

    <select id="countByCategory" resultType="long">
        SELECT COUNT(*) FROM recipe WHERE category_id = #{categoryId}
    </select>

    <select id="findHotRecipes" resultMap="DetailResultMap">
        SELECT r.*, c.name as category_name, u.real_name as author_name
        FROM recipe r
                 LEFT JOIN category c ON r.category_id = c.id
                 LEFT JOIN user u ON r.author_id = u.id
        ORDER BY r.view_count DESC LIMIT #{limit}
    </select>

    <select id="findLatestRecipes" resultMap="DetailResultMap">
        SELECT r.*, c.name as category_name, u.real_name as author_name
        FROM recipe r
                 LEFT JOIN category c ON r.category_id = c.id
                 LEFT JOIN user u ON r.author_id = u.id
        ORDER BY r.create_time DESC LIMIT #{limit}
    </select>

    <select id="getStatsByDifficulty" resultType="java.util.HashMap">
        SELECT difficulty, COUNT(*) as count FROM recipe GROUP BY difficulty
    </select>
    <select id="getRecipeCountByCategory" resultType="java.lang.Long">
        SELECT COUNT(*) FROM recipe WHERE category_id = #{categoryId}
    </select>

</mapper>