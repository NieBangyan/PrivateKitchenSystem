<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kitchen.backend_springbook_kichen.mapper.UserMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.kitchen.backend_springbook_kichen.entity.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="real_name" property="realName" />
        <result column="email" property="email" />
        <result column="phone" property="phone" />
        <result column="role" property="role" />
        <result column="status" property="status" />
        <result column="recipe_count" property="recipeCount" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 用户登录 -->
    <select id="findByUsernameAndPassword" resultMap="BaseResultMap">
        SELECT * FROM user
        WHERE username = #{username} AND password = #{password} AND status = 1
    </select>

    <!-- 根据用户名查询用户 -->
    <select id="findByUsername" parameterType="string" resultMap="BaseResultMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <!-- 根据ID查询用户 -->
    <select id="findById" parameterType="int" resultMap="BaseResultMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <!-- 查询所有用户 -->
    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM user ORDER BY id ASC
    </select>

    <!-- 搜索用户（模糊查询） -->
    <select id="search" parameterType="string" resultMap="BaseResultMap">
        SELECT * FROM user
        WHERE username LIKE CONCAT('%', #{keyword}, '%')
           OR real_name LIKE CONCAT('%', #{keyword}, '%')
           OR email LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY id ASC
    </select>

    <!-- 插入用户 -->
    <insert id="insert" parameterType="com.kitchen.backend_springbook_kichen.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user(username, password, real_name, email, phone, role, status)
        VALUES(#{username}, #{password}, #{realName}, #{email}, #{phone}, #{role}, #{status})
    </insert>

    <!-- 更新用户信息 -->
    <update id="update" parameterType="com.kitchen.backend_springbook_kichen.entity.User">
        UPDATE user
        SET real_name = #{realName},
            email = #{email},
            phone = #{phone}
        WHERE id = #{id}
    </update>

    <!-- 更新密码 -->
    <update id="updatePassword">
        UPDATE user SET password = #{password} WHERE id = #{id}
    </update>

    <!-- 删除用户 -->
    <delete id="deleteById" parameterType="int">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <!-- 更新用户状态（启用/禁用） -->
    <update id="updateStatus">
        UPDATE user SET status = #{status} WHERE id = #{id}
    </update>
    <update id="login"></update>

    <!-- 获取用户总数 -->
    <select id="count" resultType="long">
        SELECT COUNT(*) FROM user
    </select>

    <!-- 按角色统计用户数量 -->
    <select id="getStatsByRole" resultType="java.util.HashMap">
        SELECT
            role,
            COUNT(*) as count,
            SUM(CASE WHEN status = 1 THEN 1 ELSE 0 END) as active_count
        FROM user
        GROUP BY role
    </select>

    <!-- 批量删除用户 -->
    <delete id="batchDelete" parameterType="list">
        DELETE FROM user WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>