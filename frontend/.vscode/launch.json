{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "editor-browser",
            "request": "launch",
            "name": "启动前端应用",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}"
        }
    ]
}